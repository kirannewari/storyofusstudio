# Plan: Change Primary Color to Info Color in index.html

## Information Gathered:
- Found 59 instances of "primary" color classes in index.html file
- These include: text-primary, btn-primary, bg-primary, border-primary, btn-primary-outline-0
- Uses Bootstrap framework with standard color class naming
- CSS files contain no custom primary color definitions

## Plan:
### File Level Updates in index.html:
1. Replace all instances of "text-primary" with "text-info"
2. Replace all instances of "btn-primary" with "btn-info" 
3. Replace all instances of "bg-primary" with "bg-info"
4. Replace all instances of "border-primary" with "border-info"
5. Replace all instances of "btn-primary-outline-0" with "btn-info-outline-0"

### Expected Changes:
- Spinner color (text-primary → text-info)
- Navigation bars and buttons (btn-primary → btn-info)
- Social media icons (btn-primary → btn-info)
- Section headings and text (text-primary → text-info)
- Background colors (bg-primary → bg-info)
- Phone number styling (text-primary → text-info)
- Icon colors in various sections (text-primary → text-info)

## Dependent Files to be edited:
- /Users/kiran/website/storyofusstudio/index.html (main file)

## Followup steps:
1. Apply all color class changes
2. Verify the changes by opening the file in browser
3. Check that all color transitions work properly

## Categories of changes to make:
1. **Text Colors**: text-primary → text-info
2. **Button Colors**: btn-primary → btn-info
3. **Background Colors**: bg-primary → bg-info  
4. **Border Colors**: border-primary → border-info
5. **Button Outline Variants**: btn-primary-outline-0 → btn-info-outline-0
